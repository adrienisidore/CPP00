Namespace : groupe de fonctions qui ont un rapport semantique entre elles

Utiliser un maximum de fonctions de la librairie std notamment pour print
ATTENTION A ctrl+d qui peut creer une boucle infinie
PhoneBook est une boucle while(1). getline est ce qu'il y a de plus
propre pour recuperer l'entree user.
Ceci est dans la boucle while(1) :
std::getline(std::cin >> std::ws, input);
        if (std::cin.eof())//CTRL+D
        {
            std::cin.clear();
            std::cout << std::endl << "..Exiting program." << std::endl;
            break ;
        }

ex02 : il faut juste  refaire un main.cpp qui permettra d'afficher les memes choses
que 19920104_091532.log (mais avec gettimeofday() d'aujourd'hui)

-Preparer PhoneBook au propre
-Mettre code cub3d au propre
-Faire compte Mexique

https://lodev.org/cgtutor/raycasting.html#Performance